# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# The default database is SQLite, located in the server directory.
# You can change this to a different path if needed.
DB_PATH=./tlaleho_ya_nako_server.db

# Security Configuration
# A strong, random string used to sign authentication tokens.
JWT_SECRET=your_super_secret_jwt_key

# API Key
# A secret key to protect API endpoints.
API_KEY=your_secret_api_key